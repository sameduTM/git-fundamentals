## Git Fundamentals
This is a git fundamentals solo learning repository.

1. initialize a git repository.
```bash
  git init
```
2. push existing repository from cmd.
```shell
    git remote add origin https://github.com/<username>/<repo-name>.git
    # create and switch to main branch
    git branch -M main
    # push first commit
    git push -u origin main
```
